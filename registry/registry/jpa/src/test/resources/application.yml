spring:
  main:
    banner-mode: off
  datasource:
    url: "jdbc:h2:mem:jpa-${UUID.random};DB_CLOSE_DELAY=-1"
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    hibernate:
      ddl-auto: create
logging:
  level:
    root: warn
